import 'package:flutter/material.dart';

class SocialScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Social Module')),
      body: Center(child: Text('Social content goes here')),
    );
  }
}
